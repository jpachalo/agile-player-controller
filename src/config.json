{
  "sseURL": "http://localhost:8080/stream",
  "broadcastingsURL": "${broadcastingsURL}",
  "streamingsURL": "${streamingsURL}",
  "serverDateURL": "${serverDateURL}",
  "serverLastDateURL": "${serverLastDateURL}"
}
